## To start Problme3.scala:

0. Run start script to generate the points.txt file

1. Open Problme3.scala, and either define the path of the file with the points.txt file, or set as a current directory the current folder ("Problem 3") 

2. ./spark-shell

3. Paste code into spark-shell and run
